# Show list of all Packages

python3 -m pip list

# Show package details

python3 -m pip show pyjokes

# Export installed packages with versions

python3 -m pip freeze

# Search for a specific package

python3 -m pip list | grep pyjokes

which python3
python3 --version
